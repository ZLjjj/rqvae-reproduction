# GenSearchRec 项目学习总结

本文档是对整个对话历史的完整学习总结，涵盖项目架构、实验设计、关键发现和技术细节。

---

## 📖 目录

1. [项目概述](#项目概述)
2. [核心技术架构](#核心技术架构)
3. [三方对比实验设计](#三方对比实验设计)
4. [实验结果总结](#实验结果总结)
5. [硬编码层分析](#硬编码层分析)
6. [关键技术细节](#关键技术细节)
7. [工程问题与解决方案](#工程问题与解决方案)
8. [最佳实践建议](#最佳实践建议)

---

## 项目概述

### 项目目标

GenSearchRec 是一个**基于离散语义ID（Semantic ID, SID）的生成式搜索推荐系统**。

**核心思路**：
```
资源文本 
    ↓ Embedding模型
高维向量 (2560维)
    ↓ 向量量化 (RQVAE/RQ-OPQ/RQ-KMeans)
离散SID (<a_12><b_83><c_4><d_701><e_25>)
    ↓ LLM微调
用户查询 → LLM直接生成SID
    ↓ SID映射
资源详情返回
```

**优势**：
- 将连续向量检索问题转化为离散token生成问题
- LLM可以直接生成结构化的资源标识符
- 通过Trie约束保证生成的SID合法

### 目录结构

```
gensearchrec-main-0901/
├── sid/                          # 向量量化和SID生成
│   ├── src/
│   │   ├── models/              # 量化模型
│   │   │   ├── rqvae.py        # 残差向量量化
│   │   │   ├── rqvae_opq.py    # RQ-OPQ
│   │   │   ├── rqkmeans.py     # RQ-KMeans
│   │   │   └── layers.py       # 基础层(MLP, Sinkhorn等)
│   │   ├── training/           
│   │   │   └── trainer.py      # 统一训练器
│   │   ├── embedding/
│   │   │   └── processor.py    # Embedding生成
│   │   └── codebook/
│   │       └── generate_indices_rqvae.py  # SID生成
│   └── scripts/                 # 训练和评估脚本
├── sft/                          # LLM微调数据和评测
│   ├── datasets/                # SFT数据生成
│   │   └── gen_sft_dataset.py
│   └── eval/                    # 推理和评测
│       ├── infer_constrained.py # Trie约束推理
│       └── eval_sft_accuracy.py
├── llama-factory/               # LLM训练底座 (submodule)
└── results_three_way/           # 三方对比实验结果
```

---

## 核心技术架构

### 1. Embedding生成

**入口**：`sid/scripts/embedding/generate_embeddings.py`

**处理流程**：
```python
# 从JSONL读取原始资源
{
  "id": "xxx",
  "meta_main_name": "电影名",
  "meta_desc": "描述",
  "directors": ["导演"],
  "actors": ["演员"],
  "pay_type": "PAY",
  "publish_year": "2023"
}

# 规范化为 text_format
"标题：'电影名'，标签：'剧情、爱情'，付费状态：'付费'，出版时间：'2023'，导演：'xxx'，演员：'xxx'"

# 调用Embedding API
→ 2560维向量 (npy文件)
```

**关键字段映射**：
- 视频：`main_name`, `text_format`, `json_format`
- 电台：`name`, `subname`, `tags`, `text_format`

### 2. 向量量化模型

#### RQVAE (Residual Vector Quantization VAE)

**文件**：`sid/src/models/rqvae.py`

**架构**：
```
Input (2560维)
    ↓ MLP Encoder
Latent (e_dim维, 如64维)
    ↓ Layer 1: VQ (1024个码本)
Quantized_1 + Residual_1
    ↓ Layer 2: VQ (1024个码本)
Quantized_2 + Residual_2
    ↓ Layer 3: VQ (1024个码本)
Quantized_3 + Residual_3
    ↓ Layer 4: VQ (1024个码本)
Quantized_4 + Residual_4
    ↓ Layer 5: HardCodeMapper (256个码本)
Quantized_5 (硬编码 saletype)
    ↓ 求和所有量化向量
Sum of Quantized Vectors
    ↓ MLP Decoder
Reconstructed (2560维)
```

**Loss函数**：
```python
loss = recon_loss + commitment_loss
recon_loss = MSE(input, reconstructed)
commitment_loss = MSE(sg(quantized), latent)  # 停止梯度
```

**增强技术**：
- **KMeans初始化**：用训练数据的K-Means中心初始化码本
- **EMA更新**：指数移动平均更新码本，避免死码
- **Sinkhorn分配**：软分配算法，强制每个码本被均匀使用

#### RQ-OPQ (Residual Quantization with Optimized Product Quantization)

**文件**：`sid/src/models/rqvae_opq.py`

**特点**：
- L1-L4：标准残差量化
- L5：OPQ旋转优化
  - 学习正交旋转矩阵R
  - 旋转后的向量分布更均匀
  - 更适合K-Means量化

**优势**：
- 训练快（基于FAISS，3-5分钟）
- L5无硬编码，全部用于语义
- ICR=99.22%，接近最优方案

#### RQ-KMeans (FAISS)

**文件**：`sid/src/models/rqkmeans.py`

**特点**：
- 基于FAISS库的K-Means实现
- 支持GPU加速
- L5硬编码saletype

**性能**：
- 训练快（3-5分钟）
- ICR=98.36%
- L5利用率47.8%（因硬编码）

### 3. SID生成

**入口**：`sid/src/codebook/generate_indices_rqvae.py`

**流程**：
```python
# 加载训练好的模型
model.load_state_dict(checkpoint)

# 对每个embedding进行编码
for embedding in embeddings:
    indices = model.encode(embedding)  # [12, 83, 4, 701, 25]
    tokens = [f"<a_{indices[0]}>", f"<b_{indices[1]}>", ...]
    
    # 输出
    {
        "idx": 0,
        "indices": [12, 83, 4, 701, 25],
        "tokens": ["<a_12>", "<b_83>", "<c_4>", "<d_701>", "<e_25>"],
        "sid": ["<a_12>", "<b_83>", "<c_4>", "<d_701>", "<e_25>"],
        "saletype": "2"  # 如果有硬编码
    }
```

**SID格式**：
- `<a_x>`：L1码本索引（0-1023）
- `<b_x>`：L2码本索引（0-1023）
- `<c_x>`：L3码本索引（0-1023）
- `<d_x>`：L4码本索引（0-1023）
- `<e_x>`：L5码本索引（0-255或0-511）

### 4. LLM微调

**数据生成**：`sft/datasets/gen_sft_dataset.py`

**训练样本格式**：
```json
{
  "instruction": "根据用户查询、历史对话和资源类型返回资源SID",
  "input": "用户查询：'我想看科幻电影'\n历史：[]\n日期：2024-01-01",
  "output": "<a_12><b_83><c_4><d_701><e_25>",
  "rid": "request-id"
}
```

**Trie约束推理**：`sft/eval/infer_constrained.py`
```python
# 构建所有合法SID的Trie树
trie = build_trie(all_sids)

# 推理时限制logits
for token in generation:
    valid_tokens = trie.get_valid_next(current_prefix)
    logits[~valid_tokens] = -inf  # 屏蔽非法token
```

**效果**：保证LLM生成的SID一定存在于资源库中

---

## 三方对比实验设计

### 实验背景

**问题**：现有Baseline混合域训练（Station+Video）效果不佳
- ICR=96.13%（目标100%）
- CR=3.87%（目标0%）
- L1利用率41.02%（严重浪费）

**目标**：针对193,034条视频数据，找到最优SID生成方案

### 实验方案

#### 任务1：Baseline评测（仅统计）

**数据**：现有混合域baseline
**操作**：筛选video子集，重新统计指标
**目的**：建立准确的baseline参考值

#### 任务2：同源视频重训练

**数据**：`2_meta_full_video.npy` (193,034 × 2560)
- 与baseline使用相同的embedding模型
- 只包含video数据，排除station

**训练配置**：
```python
model = RQVAE(
    in_dim=2560,
    num_emb_list=[1024, 1024, 1024, 1024, 256],
    e_dim=64,
    sk_epsilons=[0, 0, 0, 0.003, 0],  # L4 Sinkhorn
    ema_decay=0.99
)
epochs = 40
```

**结果**：
- ICR=99.51% ⬆️ +3.38%
- CR=0.49% ⬇️ -87.4%
- 但L1利用率仅4.79%，L1 max_bucket=9,511

#### 任务3：数据清洗后重训练

**数据**：`3_text_format_embedding.npy`
- 标题规范化
- 标签合并去重
- 字段顺序统一
- 保留关键字段：IP、付费、出版时间、导演、演员、角色

**清洗示例**：
```
# 清洗前
text_format = "剧情,爱情,2023,李安,科幻,剧情"

# 清洗后
text_format = "标题：'电影名'，标签：'剧情、爱情、科幻'，付费状态：'付费'，出版时间：'2023'，导演：'李安'"
```

**结果**：
- ICR=99.51%（与任务2相同）
- L1 max_bucket从9,511降至6,962 ⬇️ -26.8%
- L1-L3 weighted cosine显著提升

### 优化实验：L1 Sinkhorn

**问题**：任务2和任务3的L1利用率极低

**解决方案**：在L1也启用Sinkhorn
```python
sk_epsilons = [0.003, 0, 0, 0.003, 0]  # L1+L4都用Sinkhorn
sk_iters = 100
epochs = 10  # Sinkhorn收敛快，不需要40 epochs
```

**结果（任务3+L1 Sinkhorn）**：⭐⭐⭐⭐⭐
- ICR=99.91% ⬆️ +0.4%
- CR=0.09% ⬇️ -81.6%
- L1利用率=100% ⬆️ +1197%
- L1 max_bucket=216 ⬇️ -96.9%
- **所有层利用率100%**

### 快速训练方案

**需求**：40分钟训练太慢，探索3-5分钟方案

#### RQ-OPQ方案
```python
# 基于FAISS
model = RQOPQ(
    codebook_sizes=[1024, 1024, 1024, 1024, 512],
    use_opq=True  # 最后一层用OPQ
)
```

**结果**：
- 训练时间：3-5分钟 ⚡
- ICR=99.22%
- 所有层利用率100%
- **L5无硬编码，全部用于语义**

#### RQ-KMeans方案
```python
model = RQKMeans(
    codebook_sizes=[1024, 1024, 1024, 1024, 512],
    hardcode_layer=5,  # L5硬编码saletype
)
```

**结果**：
- 训练时间：3-5分钟
- ICR=98.36%
- L5利用率47.8%（因硬编码）

---

## 实验结果总结

### 最终排名（按ICR和CR综合）

| 排名 | 方案 | ICR | CR | L1利用率 | 训练时间 | 推荐度 |
|:---:|---|---:|---:|---:|---|:---:|
| 🥇 | **任务3+L1 Sinkhorn** | **99.91%** | **0.09%** | **100%** | 40-50分钟 | ⭐⭐⭐⭐⭐ |
| 🥈 | 任务2+L1 Sinkhorn | 99.93% | 0.07% | 100% | 40-50分钟 | ⭐⭐⭐⭐ |
| 🥉 | RQ-OPQ 任务3 | 99.22% | 0.78% | 100% | **3-5分钟** | ⭐⭐⭐⭐ |
| 4 | RQ-OPQ 任务2 | 98.70% | 1.30% | 100% | 3-5分钟 | ⭐⭐⭐ |
| 5 | RQ-KMeans 任务3 | 98.36% | 1.64% | 100% | 3-5分钟 | ⭐⭐⭐ |
| 6 | 任务3原始 | 99.51% | 0.49% | 7.71% | 40分钟 | ⭐⭐ |
| 7 | 任务2原始 | 99.51% | 0.49% | 4.79% | 40分钟 | ⭐⭐ |
| 8 | Baseline混合域 | 96.13% | 3.87% | 41.02% | 40分钟 | ⭐ |

### 关键发现

#### 1. 数据清洗的价值

**对比任务2 vs 任务3（原始方案）**：
- L1 max_bucket: 9,511 → 6,962 ⬇️ 26.8%
- L1 weighted cosine: 0.545 → 0.621 ⬆️ 13.9%
- **结论**：清洗后的text_format让embedding更聚焦核心语义

#### 2. L1 Sinkhorn是关键

**对比任务3原始 vs 任务3+L1 Sinkhorn**：
- L1利用率：7.71% → 100% ⬆️ 1197%
- L1 max_bucket: 6,962 → 216 ⬇️ 96.9%
- ICR: 99.51% → 99.91% ⬆️ 0.4%
- **结论**：L1均匀化是达到100%利用率的必要条件

#### 3. FAISS方法的权衡

**RQ-OPQ优势**：
- 训练速度快（3-5分钟 vs 40-50分钟）
- 无需手动调参（自动优化）
- L5无硬编码，语义表达能力强

**RQ-OPQ劣势**：
- ICR=99.22%，略低于L1 Sinkhorn的99.91%
- 依赖FAISS库，迁移性弱

#### 4. 硬编码层的影响

**有硬编码（任务3+L1 Sinkhorn）**：
- L5强制编码saletype
- 便于业务过滤（免费/付费）
- L5利用率依赖saletype分布

**无硬编码（RQ-OPQ）**：
- L5的512个码位全部用于语义
- 灵活性强，泛化能力好
- 需依赖前4层隐式区分业务属性

---

## 硬编码层分析

详见 [hardcode_layer_analysis.md](./hardcode_layer_analysis.md)

### 快速对比

| 实验 | L5硬编码 | 硬编码内容 | 有saletype字段 |
|---|:---:|---|:---:|
| 任务3+L1 Sinkhorn | ✅ | saletype | ✅ |
| 任务2+L1 Sinkhorn | ✅ | saletype | ✅ |
| RQ-OPQ | ❌ | 无 | ❌ |
| RQ-KMeans | ✅ | saletype | ✅ |

### HardCodeMapper实现

```python
class HardCodeMapper:
    """将业务字段映射到离散索引"""
    def __init__(self, num_emb=256):
        self.num_emb = num_emb
        # saletype: 1=FREE, 2=PAY
        self.saletype_map = {
            "1": 49,  # FREE
            "2": 42,  # PAY (可能多个)
        }
    
    def encode(self, saletype):
        return self.saletype_map.get(saletype, 0)
    
    def decode(self, index):
        for key, val in self.saletype_map.items():
            if val == index:
                return key
        return None
```

---

## 关键技术细节

### 1. Sinkhorn算法

**目的**：强制码本被均匀使用

**算法**：
```python
def sinkhorn(logits, epsilon=0.003, n_iters=100):
    """
    logits: [batch, num_codebooks]
    返回: [batch, num_codebooks] 软分配概率
    """
    Q = torch.exp(logits / epsilon)
    
    for _ in range(n_iters):
        # 行归一化（每个样本的概率和=1）
        Q = Q / Q.sum(dim=1, keepdim=True)
        # 列归一化（每个码本的使用次数相等）
        Q = Q / Q.sum(dim=0, keepdim=True) * (batch_size / num_codebooks)
    
    return Q
```

**参数**：
- `epsilon=0.003`：温度参数，越小越接近硬分配
- `n_iters=100`：迭代次数，通常50-100足够

**效果**：
- 未用Sinkhorn：L1利用率7.71%，max_bucket=6,962
- 用Sinkhorn：L1利用率100%，max_bucket=216

### 2. EMA码本更新

**目的**：避免码本死亡（某些码本永远不被使用）

**算法**：
```python
# 传统VQ：直接用batch统计更新
codebook[i] = batch_vectors_assigned_to_i.mean()

# EMA VQ：指数移动平均
self.ema_count[i] = decay * self.ema_count[i] + (1-decay) * count[i]
self.ema_sum[i] = decay * self.ema_sum[i] + (1-decay) * sum[i]
self.codebook[i] = self.ema_sum[i] / (self.ema_count[i] + eps)
```

**参数**：
- `decay=0.99`：标准配置
- `eps=1e-5`：防止除零

### 3. KMeans初始化

**目的**：用数据驱动的聚类中心初始化码本

**流程**：
```python
# 1. 对训练数据采样
sample_embeddings = embeddings[:10000]

# 2. 对每一层运行KMeans
for layer in range(num_layers):
    kmeans = KMeans(n_clusters=num_emb_list[layer], n_init=100)
    kmeans.fit(sample_embeddings)
    
    # 3. 用聚类中心初始化码本
    model.layers[layer].codebook.data = kmeans.cluster_centers_
    
    # 4. 计算残差，传递给下一层
    assigned = kmeans.predict(sample_embeddings)
    quantized = kmeans.cluster_centers_[assigned]
    sample_embeddings = sample_embeddings - quantized
```

### 4. 评估指标

#### ICR (Individual Collision Rate)
```python
ICR = (unique_sids / total_samples) * 100
```
- 目标：100%
- 含义：每条资源有唯一SID

#### CR (Collision Rate)
```python
CR = ((total_samples - unique_sids) / total_samples) * 100
```
- 目标：0%
- 含义：重复SID的比例

#### Codebook利用率
```python
utilization = (active_codes / total_codes) * 100
```
- 目标：100%
- 含义：实际使用的码本占比

#### Weighted Cosine Similarity
```python
# 对每个prefix bucket
for prefix, samples in buckets:
    embeddings = get_embeddings(samples)
    # 归一化
    embeddings = embeddings / norm(embeddings)
    # 计算两两cosine
    sim_matrix = embeddings @ embeddings.T
    # 上三角平均
    bucket_sim = sim_matrix[triu_indices].mean()
    # 按bucket大小加权
    weighted_sim += bucket_sim * len(samples)

weighted_sim /= total_samples
```
- 目标：越高越好（同bucket内语义相似）
- 任务3+L1 Sinkhorn: L1=0.664, L4=0.920
- RQ-OPQ任务3: L1=0.692（最高）, L4=0.937

---

## 工程问题与解决方案

### 1. README与实际代码不一致

**问题**：
```
README引用: sid/models/rqvae.py
实际位置: sid/src/models/rqvae.py
```

**解决**：
- 更新README中的所有路径引用
- 或创建软链接保持向后兼容

### 2. SFT数据生成参数错误

**问题**：`sft/datasets/gen_sft_dataset.py`
```python
# 函数定义
def build_prompt(query, domain, ymd, asr_gender, formatted_session):
    ...

# 错误调用（缺少domain）
prompt = build_prompt(query, ymd, asr_gender, formatted_session)
```

**解决**：
```python
# 修复：添加domain参数
prompt = build_prompt(query, domain, ymd, asr_gender, formatted_session)
```

### 3. 路径硬编码

**问题**：
```python
data_path = "/lizhaoxuan1/embeddings.npy"  # Linux服务器路径
```

**解决**：
```python
# 使用argparse
parser.add_argument("--data_path", required=True)

# 或使用环境变量
data_path = os.getenv("DATA_PATH", "default_path")
```

### 4. Import路径问题

**问题**：
```python
from data.emb_dataset import EmbDataset  # 找不到
```

**原因**：代码重构后目录结构变化

**解决**：
```python
# 检查实际位置
from sid.src.models.datasets import EmbDataset
```

### 5. HardCodeMapper不一致

**问题**：
- `processor.py`: PAY=1
- `rqvae.py`: PAY=42

**解决**：统一映射逻辑
```python
# 统一配置文件
SALETYPE_MAPPING = {
    "FREE": 49,
    "PAY": 42,
}
```

---

## 最佳实践建议

### 1. 选择合适的方案

#### 场景A：追求极致精度，不在乎训练时间
**推荐**：任务3+L1 Sinkhorn
- ICR=99.91%（最高）
- 所有层100%利用率
- L5硬编码便于业务过滤
- 训练时间40-50分钟

#### 场景B：需要快速迭代，接受略低精度
**推荐**：RQ-OPQ 任务3
- ICR=99.22%（足够高）
- 训练仅3-5分钟
- L5无硬编码，灵活性强
- 基于FAISS，代码简洁

#### 场景C：需要业务字段强约束
**推荐**：RQ-KMeans 任务3
- L5硬编码saletype
- 训练3-5分钟
- ICR=98.36%

### 2. 数据预处理

**必做**：
1. 标题规范化（去空格、统一格式）
2. 标签去重排序
3. 字段顺序统一
4. 保留核心字段（IP、导演、演员、时间、付费状态）

**可选**：
- 同义词合并（"爱情" = "浪漫"）
- 停用词过滤
- 长描述截断

### 3. 训练参数推荐

#### RQVAE + Sinkhorn
```python
{
    "in_dim": 2560,
    "num_emb_list": [1024, 1024, 1024, 1024, 256],
    "e_dim": 64,
    "sk_epsilons": [0.003, 0, 0, 0.003, 0],  # L1+L4
    "sk_iters": 100,
    "kmeans_init": True,
    "kmeans_iters": 100,
    "ema_decay": 0.99,
    "lr": 1e-3,
    "batch_size": 1024,
    "epochs": 10,  # Sinkhorn收敛快
    "optimizer": "adamw",
    "weight_decay": 1e-4
}
```

#### RQ-OPQ (FAISS)
```python
{
    "codebook_sizes": [1024, 1024, 1024, 1024, 512],
    "use_opq": True,
    "opq_dim": 64,
    "niter": 50,
    "gpu": True
}
```

### 4. 评估流程

```python
# 1. 基础指标
compute_icr_cr(indices)
compute_utilization(indices, num_emb_list)

# 2. 分布分析
analyze_prefix_distribution(indices)
plot_bucket_sizes(indices)

# 3. 语义一致性
compute_weighted_cosine(embeddings, indices)

# 4. 重构质量
mse = compute_reconstruction_error(embeddings, reconstructed)
```

### 5. 部署建议

#### 离线索引生成
```bash
# 1. 生成embeddings
python sid/scripts/embedding/generate_embeddings.py \
    --inputs data/resources.jsonl \
    --output_prefix data/embeddings

# 2. 生成SID索引
python sid/src/codebook/generate_indices_rqvae.py \
    --embedding_file data/embeddings.npy \
    --checkpoint checkpoints/best_loss.pt \
    --output data/indices.jsonl

# 3. 构建SID→资源映射
python build_sid_mapping.py \
    --indices data/indices.jsonl \
    --resources data/resources.jsonl \
    --output data/sid_mapping.json
```

#### LLM推理
```python
# 1. 构建Trie
trie = build_trie_from_indices(indices)

# 2. 约束生成
output_sids = model.generate(
    user_query,
    max_new_tokens=50,
    logits_processor=[TrieConstraint(trie)]
)

# 3. SID反查
resources = [sid_mapping[sid] for sid in output_sids]
```

---

## 总结

GenSearchRec项目通过**向量量化+LLM生成**的范式，将传统的向量检索问题转化为结构化ID生成问题。

**核心创新**：
1. 离散SID：连续向量 → 离散token序列
2. LLM直接生成：查询 → SID（无需向量检索）
3. Trie约束：保证生成合法

**关键技术**：
- RQVAE残差量化
- Sinkhorn均匀化
- EMA防死码
- KMeans初始化
- HardCode业务字段

**最优方案**：
- **精度优先**：任务3+L1 Sinkhorn (ICR=99.91%, 40-50分钟)
- **速度优先**：RQ-OPQ (ICR=99.22%, 3-5分钟)

**实验收获**：
1. 数据清洗价值巨大（-26.8% max_bucket）
2. L1 Sinkhorn是100%利用率的关键
3. FAISS方法在速度和精度间取得良好平衡
4. 硬编码层适合业务约束强的场景

---

## 参考资源

- [项目README](./README.md)
- [实验完整记录](./实验完整记录.md)
- [实验对比表格](./实验对比表格.md)
- [硬编码层分析](./hardcode_layer_analysis.md)
- [SID三方对比实验计划.docx](./SID三方对比实验计划.docx)
