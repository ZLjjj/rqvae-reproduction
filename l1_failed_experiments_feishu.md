---

## 失败与不推荐的增量实验

| 实验 | 修改 | 关键结果 | 判断 |
|---|---|---|---|
| 实验 B | `num_emb_list=[256,1024,1024,1024,256]` | L1 仅使用 6/256 个 code，利用率 2.34%；L1 最大频次 45,311；完整 CR=0.6999% | 不采用，L1 更集中，完整 SID 劣于原任务2 |
| 实验 C | `num_emb_list=[128,1024,1024,1024,256]` | L1 使用 14/128 个 code，利用率 10.94%；L1 最大频次 19,169；完整 CR=0.5548% | 不采用，仍劣于原任务2 |
| 实验 D | L1=256，L1 epsilon=0.003 | L1 利用率 100%，但 L3/L4 利用率降至 38.38%/38.96%，L4 最大频次 47,502；完整 CR=0.4056% | 暂不采用，后层严重集中 |
| L1 epsilon=0.001 | 原 codebook，L1 epsilon=0.001 | 首轮训练 Sinkhorn 返回 NaN/Inf，实验中止 | 不采用，epsilon 过小导致数值不稳定 |

### 简要结论

单纯缩小 L1 codebook 不能解决利用率问题；缩小 L1 并叠加 Sinkhorn 会把容量压力转移到后层；epsilon=0.001 在当前实现下不稳定。因此这些实验仅作为负面证据保留。
