# GenSearchRec 快速参考手册

本文档提供项目的快速查询和常用命令参考。

---

## 📌 项目一句话总结

**将高维资源向量压缩为离散SID token，让LLM直接生成SID完成检索推荐。**

```
用户查询 → LLM → <a_12><b_83><c_4><d_701><e_25> → 资源详情
```

---

## 🎯 最佳实验方案速查

### 精度最优 (99.91% ICR)
```bash
# 任务3 + L1 Sinkhorn
实验路径: results_three_way/exp_task3_l1_sinkhorn/
数据: 3_text_format_embedding.npy
配置: [1024,1024,1024,1024,256] + L1+L4 Sinkhorn
训练时间: 40-50分钟
特点: 所有层100%利用率，L5硬编码saletype
```

### 速度最优 (3-5分钟)
```bash
# RQ-OPQ 任务3
实验路径: results_three_way/rqopq_task3/
数据: 3_text_format_embedding.npy
配置: [1024,1024,1024,1024,512] + FAISS OPQ
ICR: 99.22%
特点: L5无硬编码，全语义表达
```

---

## 📁 关键文件路径

### 模型代码
```
sid/src/models/rqvae.py          # RQVAE主模型
sid/src/models/rqvae_opq.py      # RQ-OPQ模型
sid/src/models/rqkmeans.py       # RQ-KMeans模型
sid/src/models/layers.py         # Sinkhorn等基础层
```

### 训练脚本
```
sid/scripts/train/train_rqvae.py         # RQVAE训练
sid/scripts/train/train_rqvae_opq.py     # RQ-OPQ训练
sid/scripts/train/train_rqkmeans_faiss.py  # RQ-KMeans训练
```

### 数据生成
```
sid/scripts/embedding/generate_embeddings.py    # Embedding生成
sid/src/codebook/generate_indices_rqvae.py      # SID索引生成
sft/datasets/gen_sft_dataset.py                 # LLM训练数据
```

### 评估工具
```
sid/src/codebook/metrics.py              # ICR/CR等指标
sft/eval/infer_constrained.py           # Trie约束推理
sft/eval/eval_sft_accuracy.py           # 准确率评估
```

---

## 🚀 常用命令

### 1. 生成Embedding
```bash
python sid/scripts/embedding/generate_embeddings.py \
    --inputs data/resources.jsonl \
    --output_prefix data/embeddings \
    --text_field meta \
    --max_workers 64

# 输出:
# data/embeddings.npy (2560维向量)
# data/embeddings_meta.jsonl (元数据)
```

### 2. 训练RQVAE (L1 Sinkhorn)
```bash
python sid/scripts/train/train_rqvae.py \
    --data_path data/embeddings.npy \
    --ckpt_dir checkpoints/rqvae_l1_sinkhorn \
    --num_emb_list 1024 1024 1024 1024 256 \
    --sk_epsilons 0.003 0 0 0.003 0 \
    --sk_iters 100 \
    --kmeans_init \
    --kmeans_iters 100 \
    --ema_decay 0.99 \
    --epochs 10 \
    --batch_size 1024 \
    --lr 1e-3 \
    --device cuda

# 输出:
# checkpoints/rqvae_l1_sinkhorn/best_loss.pt
# checkpoints/rqvae_l1_sinkhorn/best_collision.pt
```

### 3. 训练RQ-OPQ (快速)
```bash
python sid/scripts/train/train_rqkmeans_opq_faiss.py \
    --data_path data/embeddings.npy \
    --output_dir checkpoints/rqopq \
    --codebook_sizes 1024 1024 1024 1024 512 \
    --use_opq \
    --gpu

# 3-5分钟完成
```

### 4. 生成SID索引
```bash
python sid/src/codebook/generate_indices_rqvae.py \
    --embedding_file data/embeddings.npy \
    --meta_file data/embeddings_meta.jsonl \
    --checkpoint checkpoints/rqvae_l1_sinkhorn/best_loss.pt \
    --output_file data/indices.jsonl \
    --batch_size 512 \
    --device cuda

# 输出:
# data/indices.jsonl
# 每行: {"idx":0, "indices":[12,83,4,701,25], "tokens":[...], "saletype":"2"}
```

### 5. 生成SFT训练数据
```bash
python sft/datasets/gen_sft_dataset.py \
    --indices_file data/indices.jsonl \
    --train_csv data/train_queries.csv \
    --output_file data/sft_train.jsonl \
    --dataset_name my_sft_dataset

# 输出:
# data/sft_train.jsonl
# llama-factory/data/dataset_info.json (自动更新)
```

### 6. LLM微调 (LLaMA-Factory)
```bash
cd llama-factory

llamafactory-cli train \
    --stage sft \
    --model_name_or_path /path/to/llama \
    --dataset my_sft_dataset \
    --template llama3 \
    --finetuning_type lora \
    --output_dir checkpoints/llama_sft \
    --per_device_train_batch_size 4 \
    --num_train_epochs 3 \
    --learning_rate 5e-5
```

### 7. Trie约束推理
```bash
python sft/eval/infer_constrained.py \
    --model_path checkpoints/llama_sft \
    --indices_file data/indices.jsonl \
    --test_file data/test_queries.csv \
    --output_file results/predictions.jsonl

# 生成的SID保证在indices.jsonl中存在
```

### 8. 评估准确率
```bash
python sft/eval/eval_sft_accuracy.py \
    --predictions results/predictions.jsonl \
    --ground_truth data/test_queries.csv \
    --indices_file data/indices.jsonl

# 输出: Recall@1, Recall@5, Recall@10等指标
```

---

## 📊 关键指标速查

### ICR (Individual Collision Rate)
```python
ICR = unique_sids / total_samples * 100
# 目标: 100%
# 最优: 99.91% (任务3+L1 Sinkhorn)
```

### CR (Collision Rate)
```python
CR = (total_samples - unique_sids) / total_samples * 100
# 目标: 0%
# 最优: 0.07% (任务2+L1 Sinkhorn)
```

### Codebook利用率
```python
utilization = active_codes / total_codes * 100
# 目标: 100%
# L1利用率: Baseline=41% → L1 Sinkhorn=100%
```

### Weighted Cosine
```python
# 同prefix bucket内embedding的平均余弦相似度
# 目标: 越高越好 (0.6-0.9为优秀)
# 最优: L1=0.692 (RQ-OPQ任务3)
```

---

## ⚙️ 核心参数配置

### RQVAE标准配置
```python
{
    "in_dim": 2560,                    # embedding维度
    "num_emb_list": [1024,1024,1024,1024,256],  # 5层码本
    "e_dim": 64,                       # latent维度
    "layers": [512, 256],              # MLP隐藏层
    
    # Sinkhorn
    "sk_epsilons": [0.003, 0, 0, 0.003, 0],  # L1+L4
    "sk_iters": 100,
    
    # KMeans初始化
    "kmeans_init": True,
    "kmeans_iters": 100,
    
    # EMA
    "ema_decay": 0.99,
    "ema_eps": 1e-5,
    
    # 训练
    "lr": 1e-3,
    "batch_size": 1024,
    "epochs": 10,                      # Sinkhorn收敛快
    "optimizer": "adamw",
    "weight_decay": 1e-4,
}
```

### RQ-OPQ (FAISS) 配置
```python
{
    "codebook_sizes": [1024,1024,1024,1024,512],
    "use_opq": True,
    "opq_dim": 64,                     # OPQ旋转维度
    "niter": 50,                       # K-Means迭代
    "gpu": True,
}
```

### Sinkhorn参数影响
```python
epsilon = 0.003   # 标准 (推荐)
epsilon = 0.001   # 更强均匀化，但可能不稳定
epsilon = 0.01    # 更软分配，效果略差
epsilon = 0       # 硬分配 (等价于不用Sinkhorn)
```

---

## 🔍 常见问题速查

### Q1: L1利用率低怎么办？
**A**: 在L1启用Sinkhorn
```python
sk_epsilons = [0.003, 0, 0, 0.003, 0]  # L1+L4都用
```
效果: 4.79% → 100%

### Q2: 训练太慢怎么办？
**A**: 使用RQ-OPQ (FAISS)
```bash
python sid/scripts/train/train_rqkmeans_opq_faiss.py
```
速度: 40分钟 → 3-5分钟

### Q3: 需要业务字段约束怎么办？
**A**: 启用L5硬编码
```python
use_hardcode_layer = True
hardcode_fields = ['saletype']
```

### Q4: 硬编码和无硬编码如何选择？
**A**: 
- 有硬编码 (RQVAE): 业务过滤方便，L5利用率依赖业务分布
- 无硬编码 (RQ-OPQ): 语义表达能力强，灵活性高

### Q5: ICR到99%还能提升吗？
**A**: 
1. 数据清洗 (任务2→任务3: +0%)
2. L1 Sinkhorn (任务3原始→L1 Sinkhorn: +0.4%)
3. 增加码本大小 (1024→2048)
4. 增加层数 (5层→6层)

### Q6: 如何验证SID质量？
**A**: 
```bash
# 1. 基础指标
compute_icr_cr()

# 2. 分布分析
analyze_prefix_distribution()

# 3. 语义一致性
compute_weighted_cosine()

# 4. 可视化
plot_bucket_sizes()
plot_utilization()
```

---

## 🎨 SID格式说明

### 标准5层SID
```
<a_853><b_475><c_672><d_353><e_42>

<a_x>: L1码本索引 (0-1023)
<b_x>: L2码本索引 (0-1023)
<c_x>: L3码本索引 (0-1023)
<d_x>: L4码本索引 (0-1023)
<e_x>: L5码本索引 (0-255 或 0-511)
```

### 有硬编码的SID
```json
{
  "indices": [853, 475, 672, 353, 42],
  "tokens": ["<a_853>", "<b_475>", "<c_672>", "<d_353>", "<e_42>"],
  "saletype": "2"
}

<e_42>: 对应 saletype="2" (付费内容)
<e_49>: 对应 saletype="1" (免费内容)
```

### 无硬编码的SID
```json
{
  "indices": [853, 475, 672, 747, 824],
  "tokens": ["<a_853>", "<b_475>", "<c_672>", "<d_747>", "<e_824>"]
}

<e_x>: 纯语义编码，取值范围0-511
```

---

## 📈 实验结果对比表

| 方案 | ICR | CR | L1利用率 | L1 max | 训练时间 |
|---|---:|---:|---:|---:|---|
| **任务3+L1 Sinkhorn** | **99.91%** | **0.09%** | **100%** | **216** | 40-50分钟 |
| 任务2+L1 Sinkhorn | 99.93% | 0.07% | 100% | 210 | 40-50分钟 |
| RQ-OPQ 任务3 | 99.22% | 0.78% | 100% | - | **3-5分钟** |
| RQ-KMeans 任务3 | 98.36% | 1.64% | 100% | - | 3-5分钟 |
| 任务3原始 | 99.51% | 0.49% | 7.71% | 6,962 | 40分钟 |
| 任务2原始 | 99.51% | 0.49% | 4.79% | 9,511 | 40分钟 |
| Baseline混合域 | 96.13% | 3.87% | 41.02% | 3,065 | 40分钟 |

---

## 🛠️ 调试技巧

### 检查码本死亡
```python
# 统计每个码本的使用次数
code_counts = Counter(indices[:, layer_idx])
dead_codes = [i for i in range(num_emb) if i not in code_counts]
print(f"Dead codes: {len(dead_codes)}/{num_emb}")
```

### 可视化bucket分布
```python
import matplotlib.pyplot as plt

bucket_sizes = [len(samples) for prefix, samples in prefix_groups.items()]
plt.hist(bucket_sizes, bins=50)
plt.xlabel('Bucket Size')
plt.ylabel('Count')
plt.title(f'L{layer} Bucket Distribution')
plt.show()
```

### 检查重构质量
```python
reconstructed = model(embeddings)
mse = ((embeddings - reconstructed) ** 2).mean()
print(f"Reconstruction MSE: {mse:.6f}")

# 目标: <0.01 (RQVAE通常0.005左右)
```

### 分析碰撞样本
```python
# 找出重复的SID
sid_to_samples = defaultdict(list)
for idx, sid in enumerate(sids):
    sid_to_samples[sid].append(idx)

collisions = {sid: samples for sid, samples in sid_to_samples.items() 
              if len(samples) > 1}

print(f"Total collisions: {len(collisions)}")
for sid, samples in list(collisions.items())[:5]:
    print(f"\nSID: {sid}")
    for idx in samples:
        print(f"  - {metadata[idx]['meta_main_name']}")
```

---

## 📚 延伸阅读

- [学习总结_完整版.md](./学习总结_完整版.md) - 详细技术总结
- [hardcode_layer_analysis.md](./hardcode_layer_analysis.md) - 硬编码层分析
- [实验完整记录.md](./实验完整记录.md) - 所有实验记录
- [实验对比表格.md](./实验对比表格.md) - 实验结果对比

---

## 🚨 注意事项

1. **路径检查**: 运行前确认所有路径正确（避免硬编码Linux路径）
2. **依赖版本**: PyTorch ≥ 1.12, FAISS ≥ 1.7 (RQ-OPQ需要)
3. **内存占用**: 193k样本×2560维 ≈ 4GB，确保GPU内存足够
4. **Sinkhorn稳定性**: epsilon太小(<0.001)可能不收敛
5. **硬编码字段**: 确保processor.py和rqvae.py的映射一致
6. **JSONL对齐**: embeddings.npy和meta.jsonl必须严格按行对齐

---

## 📞 快速联系

- 项目仓库: (填写实际地址)
- 问题反馈: (填写issue地址)
- 文档维护: (填写负责人)

---

**最后更新**: 2024-01-01  
**版本**: v1.0
