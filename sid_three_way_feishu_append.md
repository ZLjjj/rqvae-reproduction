---

## 11 与实验总表对齐的指标

以下表格沿用实验总表的列定义。冲突率与独立编码率取完整 5 层 SID；内聚性取共享 L1/L2/L3 prefix 的 weighted cosine，格式为“共享一层 / 共享两层 / 共享三层”。

| 基础Embedding模型 | SID生成方法 | 关键参数设置 | 梯度更新方式 | 冲突率 | 独立编码率 | 总体内聚性 | 视频内聚性 | 电台内聚性 |
|---|---|---|---|---:|---:|---|---|---|
| Qwen3-Embedding-4B | RQ-VAE + 硬编码（baseline 视频子集） | 4层1024维 + 1层hard-code；混合域训练后取视频 | EMA（decay=0.99） | 0.038667 | 0.961333 | 0.575612 / 0.667208 / 0.755617 | 0.575612 / 0.667208 / 0.755617 | 不适用（本次按视频子集评测） |
| Qwen3-Embedding-4B | RQ-VAE + 硬编码（任务2 同源 video-only） | 4层1024维 + 1层hard-code；video-only | EMA（decay=0.99） | **0.004875** | **0.995125** | 0.547222 / 0.695476 / 0.839722 | 0.547222 / 0.695476 / 0.839722 | 不适用（纯视频训练） |
| Qwen3-Embedding-4B cleaned text_format | RQ-VAE + 硬编码（任务3 video-only） | 4层1024维 + 1层hard-code；清洗重组 text_format | EMA（decay=0.99） | 0.004890 | 0.995110 | **0.620066 / 0.755967 / 0.850521** | **0.620066 / 0.755967 / 0.850521** | 不适用（纯视频训练） |

### 11.1 与原实验表的口径说明

- `冲突率 = 1 - ICR`，这里使用完整 5 层 SID 的 unique path 统计。
- `独立编码率 = ICR`，表示完整 SID 路径唯一的资源比例。
- `总体内聚性`在本三方实验中等同于视频内聚性，因为三套结果都只对视频资源进行评测；没有将 station 样本混入计算。
- `电台内聚性`不适用于任务2、任务3，也不应从混合 baseline 的 station 结果直接填充到视频-only 对比表中。
- 内聚性数值为 prefix bucket 内 embedding 两两 cosine 的样本对数加权平均；第5层 hard-code 不参与 cosine 计算。

### 11.2 对齐后的结论

按实验总表的“冲突率 / 独立编码率 / 内聚性”三类指标看，任务2和任务3都显著优于 baseline。任务2在独立编码率上略高（0.995125 vs 0.995110）；任务3在 L1-L3 视频内聚性上更高，且 L1 最大 bucket 从任务2的 9,511 降至 6,962，因此更适合关注前缀召回和分桶结构的下游场景。
